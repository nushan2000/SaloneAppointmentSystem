apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
        - name: auth-service
          image: nushan2000/auth-service:latest
          ports:
            - containerPort: 4001
          env:
            - name: PORT
              value: "4001"
            - name: JWT_SECRET
              value: "nushanhansana"
            - name: KAFKA_BROKER
              value: "kafka-service:9092"
            - name: MONGO_URI_AUTH
              value: "mongodb://mongodb:27017/salone"
